{% extends 'base.html.twig' %}

{% block title %}Tournage{% endblock %}

{% block body %}
    <h1>Tournage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournage.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ tournage.dateDebut }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ tournage.dateFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tournage_index') }}">back to list</a>

    <a href="{{ path('app_tournage_edit', {'id': tournage.id}) }}">edit</a>

    {{ include('tournage/_delete_form.html.twig') }}
{% endblock %}
