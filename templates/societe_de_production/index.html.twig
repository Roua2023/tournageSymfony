{% extends 'base.html.twig' %}

{% block title %}SocieteDeProduction index{% endblock %}

{% block body %}
<div style="margin-left:10%">
    <h1>SocieteDeProduction index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Adresse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for societe_de_production in societe_de_productions %}
            <tr>
                <td>{{ societe_de_production.id }}</td>
                <td>{{ societe_de_production.nom }}</td>
                <td>{{ societe_de_production.adresse }}</td>
                <td>
                    <a href="{{ path('app_societe_de_production_show', {'id': societe_de_production.id}) }}">show</a>
                    <a href="{{ path('app_societe_de_production_edit', {'id': societe_de_production.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_societe_de_production_new') }}">Create new</a>
</div>
{% endblock %}
